import React from "react";
import { ErrorComponent } from "./ErrorComponent";

export default {
  title: "Stage/ErrorComponent",
  component: ErrorComponent,
};

export function Component(): JSX.Element {
  return <ErrorComponent />;
}
